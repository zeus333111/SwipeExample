<resources>
    <!-- Base application theme. -->
    <style name="Theme.SwipeExample" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!--Aqui se define el estilo para todos los til y tiet-->
        <item name="textInputStyle">@style/OutLinedText</item>
    </style>

    <style name="Theme.AppSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.SwipeExample</item>

        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/teal_200</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="OutLinedText">
        <item name="materialThemeOverlay">@style/TextInputs_overlay</item>
        <item name="shapeAppearance">@style/TexFieldsShape</item>
    </style>

    <style name="TextInputs_overlay" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="colorPrimary">@color/yellow</item>
        <item name="colorSurface">@color/red</item>
    </style>

    <style name="TexFieldsShape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">25dp</item>
    </style>

</resources>